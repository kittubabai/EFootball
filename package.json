{
  "name": "efootball-tournament-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack eFootball tournament web application with TypeScript Express backend and React frontend",
  "scripts": {
    "install:all": "npm install --include=dev --prefix frontend && npm install --include=dev --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  }
}
